services:
  - type: web
    name: ai-phishing-backend
    env: python
    region: singapore
    plan: free
    runtime: python3.11  # Add this line
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt --no-cache-dir"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
